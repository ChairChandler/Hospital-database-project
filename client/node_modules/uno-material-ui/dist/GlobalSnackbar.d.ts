import * as React from 'react';
export interface GlobalSnackbarProps {
    message: {
        messageText: string;
        messageType: string;
    };
    seconds: number;
    mobile: boolean;
}
export declare const GlobalSnackbar: React.FunctionComponent<GlobalSnackbarProps>;
