export default class GridResponse {
    aggregationPayload: object;
    counter: number;
    currentPage: number;
    filteredRecordCount: number;
    payload: any[];
    totalPages: number;
    totalRecordCount: number;
    constructor(counter?: number);
}
